<!DOCTYPE html>
<html class="lib-textFormatted">
<head>
  <title>Markin</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%= stylesheet_link_tag 'application', media: 'all', 'shim-shadowdom' => 'yes' %>
  <%= javascript_include_tag 'application' %>
  <script>
    I18n.locale = <%= raw I18n.locale.to_json %>;
    I18n.enabledLocales = <%= raw Rails.application.config.i18n_enabled_locales.to_json %>;
  </script>
  <%= html_import_tag 'fullpicture' %>
  <%= html_import_tag 'app' %>
  <%= csrf_meta_tags %>
</head>
<body unresolved>
  
  <lib-layout modules="24" class="add-header">
    <app-stickyHeader></app-stickyHeader>
    <%= render 'lib/site_title' %>
    <%= render 'lib/menu' %>
  </lib-layout>

  <lib-layout modules="24">
    <% if content_for?(:main_content) %>
      <%= yield :main_content %>
    <% else %>
      <%= yield %>
    <% end %>
  </lib-layout>
</body>
</html>
